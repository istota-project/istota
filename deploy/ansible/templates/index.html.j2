<!DOCTYPE html>
<!-- Placeholder written by Ansible only when index.html is absent. Replace it
     with your own content; the playbook will not overwrite it again. -->
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="robots" content="noindex, nofollow" />
    <title>{{ istota_hostname }}</title>
    <style>
      body {
        font-family: system-ui, sans-serif;
        color: #333;
        background: #fff;
        margin: 0;
        padding: 3rem 1.5rem;
        text-align: center;
      }
      @media (prefers-color-scheme: dark) {
        body { color: #ddd; background: #161616; }
      }
      h1 { font-size: 1.5rem; font-weight: 600; }
      p { color: #888; }
    </style>
  </head>
  <body>
    <h1>{{ istota_hostname }}</h1>
    <p>Nothing to see here.</p>
  </body>
</html>
