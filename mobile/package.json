{
  "name": "istota-mobile",
  "version": "0.1.0",
  "private": true,
  "description": "Capacitor shell for the Istota web UI, plus the in-house background location tracker.",
  "type": "module",
  "scripts": {
    "sync": "cap sync ios",
    "open": "cap open ios",
    "run:sim": "cap run ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.4.2",
    "@capacitor/ios": "^8.4.2"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.4.2",
    "dotenv": "^17.2.3",
    "typescript": "^5.9.3"
  }
}
